---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: permissionstemplates.iam.sonarqube.crossplane.io
spec:
  group: iam.sonarqube.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - sonarqube
    kind: PermissionsTemplate
    listKind: PermissionsTemplateList
    plural: permissionstemplates
    singular: permissionstemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: A PermissionsTemplate is an example API type.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A PermissionsTemplateSpec defines the desired state of a
              PermissionsTemplate.
            properties:
              forProvider:
                description: PermissionsTemplateParameters are the configurable fields
                  of a PermissionsTemplate.
                properties:
                  creatorPermissions:
                    description: CreatorPermissions is a list of permissions that
                      apply to the creator of the project. This is an optional field
                      that allows you to specify permissions for the project creator.
                    items:
                      enum:
                      - admin
                      - codeviewer
                      - issueadmin
                      - securityhotspotadmin
                      - scan
                      - user
                      type: string
                    type: array
                  default:
                    description: Default is a boolean field that indicates whether
                      this PermissionsTemplate is the default template for new projects.
                      This is an optional field that can be set to true if you want
                      this template to be used as the default for new projects.
                    type: boolean
                  description:
                    description: Description of the PermissionsTemplate. This is an
                      optional field that provides additional information about the
                      PermissionsTemplate.
                    type: string
                  groupPermissions:
                    description: GroupPermissions is a list of permissions assigned
                      to specific groups in the context of the PermissionsTemplate.
                      This is an optional field that allows you to specify group-specific
                      permissions for the PermissionsTemplate.
                    items:
                      description: PermissionsTemplateGroupParameters is a struct
                        that represents the permissions assigned to a group in a SonarQube
                        Permission Template.
                      properties:
                        name:
                          description: Name is the name of the group in SonarQube.
                          minLength: 1
                          type: string
                        permissions:
                          description: |-
                            Permissions is a list of permissions assigned to the group. An empty list means that the group will be associated with the PermissionsTemplate but will not have any permissions.
                            Allowed values are "admin", "codeviewer", "issueadmin", "securityhotspotadmin", "scan", "user"
                          items:
                            enum:
                            - admin
                            - codeviewer
                            - issueadmin
                            - securityhotspotadmin
                            - scan
                            - user
                            type: string
                          type: array
                      required:
                      - name
                      type: object
                    type: array
                  name:
                    description: Name of the PermissionsTemplate. This is a required
                      field and must be unique within the SonarQube instance.
                    minLength: 1
                    type: string
                  projectKeyPattern:
                    description: ProjectKeyPattern is a regex pattern that defines
                      which projects the PermissionsTemplate applies to. This is an
                      optional field that allows you to specify a pattern for matching
                      project keys.
                    type: string
                  userPermissions:
                    description: UserPermissions is a list of permissions assigned
                      to specific users in the context of the PermissionsTemplate.
                      This is an optional field that allows you to specify user-specific
                      permissions for the PermissionsTemplate.
                    items:
                      description: PermissionsTemplateUserParameters is a struct that
                        represents the permissions assigned to a user in a SonarQube
                        Permission Template.
                      properties:
                        login:
                          description: Login is the unique identifier of the user
                            in SonarQube. This field is required to associate the
                            permissions with a specific user.
                          minLength: 1
                          type: string
                        permissions:
                          description: |-
                            Permissions is a list of permissions assigned to the user. This field is required and specifies the permissions the user has in the context of the Permission Template.
                            Allowed values are "admin", "codeviewer", "issueadmin", "securityhotspotadmin", "scan", "user"
                          items:
                            enum:
                            - admin
                            - codeviewer
                            - issueadmin
                            - securityhotspotadmin
                            - scan
                            - user
                            type: string
                          type: array
                      required:
                      - login
                      - permissions
                      type: object
                    type: array
                required:
                - name
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A PermissionsTemplateStatus represents the observed state
              of a PermissionsTemplate.
            properties:
              atProvider:
                description: PermissionsTemplateObservation are the observable fields
                  of a PermissionsTemplate.
                properties:
                  createdAt:
                    description: CreatedAt is the timestamp when the PermissionsTemplate
                      was created in SonarQube. This field is set by the SonarQube
                      API and reflects the actual creation time of the PermissionsTemplate.
                    format: date-time
                    type: string
                  creatorPermissions:
                    description: CreatorPermissions is a list of permissions that
                      apply to the creator of the project. This reflects the actual
                      permissions for the project creator in SonarQube.
                    items:
                      type: string
                    type: array
                  default:
                    description: Default is a boolean field that indicates whether
                      this PermissionsTemplate is the default template for new projects.
                      This reflects the actual default status of the PermissionsTemplate
                      in SonarQube.
                    type: boolean
                  description:
                    description: Description is the description of the PermissionsTemplate.
                      This reflects the actual description of the PermissionsTemplate
                      in SonarQube.
                    type: string
                  groupPermissions:
                    description: GroupPermissions is a list of permissions assigned
                      to specific groups in the context of the PermissionsTemplate.
                      This reflects the actual group-specific permissions for the
                      PermissionsTemplate in SonarQube.
                    items:
                      description: PermissionsTemplateGroupObservation is a struct
                        that represents the observed state of a group in a SonarQube
                        Permission Template. It contains the actual name of the group
                        and the permissions assigned to it as observed from the SonarQube
                        API response.
                      properties:
                        name:
                          description: Name is the name of the group in SonarQube.
                            This field is observed from the SonarQube API and reflects
                            the actual name of the group associated with the PermissionsTemplate.
                          type: string
                        permissions:
                          description: Permissions represents the list of permissions
                            assigned to the group in the context of the PermissionsTemplate.
                            This field is observed from the SonarQube API and reflects
                            the actual permissions assigned to the group for the PermissionsTemplate.
                          items:
                            type: string
                          type: array
                      required:
                      - name
                      - permissions
                      type: object
                    type: array
                  id:
                    description: ID is the unique identifier of the PermissionsTemplate
                      in SonarQube.  This field is created by the SonarQube API when
                      the PermissionsTemplate is created.
                    type: string
                  name:
                    description: Name is the name of the PermissionsTemplate. This
                      reflects the actual name of the PermissionsTemplate in SonarQube.
                    type: string
                  projectKeyPattern:
                    description: ProjectKeyPattern is a regex pattern that defines
                      which projects the PermissionsTemplate applies to. This reflects
                      the actual pattern of the PermissionsTemplate in SonarQube.
                    type: string
                  updatedAt:
                    description: UpdatedAt is the timestamp when the PermissionsTemplate
                      was last updated in SonarQube. This field is set by the SonarQube
                      API and reflects the actual last update time of the PermissionsTemplate.
                    format: date-time
                    type: string
                  userPermissions:
                    description: UserPermissions is a list of permissions assigned
                      to specific users in the context of the PermissionsTemplate.
                      This reflects the actual user-specific permissions for the PermissionsTemplate
                      in SonarQube.
                    items:
                      description: PermissionsTemplateUserObservation is a struct
                        that represents the observed permissions for a user in a SonarQube
                        Permission Template.
                      properties:
                        login:
                          description: Login is the unique identifier of the user
                            in SonarQube. This field is observed from the SonarQube
                            API and reflects the actual login of the user associated
                            with the PermissionsTemplate.
                          type: string
                        permissions:
                          description: Permissions is a list of permissions assigned
                            to the user in the context of the PermissionsTemplate.
                            This field is observed from the SonarQube API and reflects
                            the actual permissions assigned to the user for the PermissionsTemplate.
                          items:
                            type: string
                          type: array
                      required:
                      - login
                      - permissions
                      type: object
                    type: array
                required:
                - default
                - description
                - id
                - name
                - projectKeyPattern
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
