---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: groups.iam.sonarqube.crossplane.io
spec:
  group: iam.sonarqube.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - sonarqube
    kind: Group
    listKind: GroupList
    plural: groups
    singular: group
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          A Group is the Schema for the Groups API. It represents a
          Group resource in SonarQube, which is a collection of users that can be
          assigned permissions to access and manage resources in SonarQube.
          The Group resource allows you to define and manage groups of users in
          SonarQube, including their names, descriptions, and permissions.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A GroupSpec defines the desired state of a Group.
            properties:
              forProvider:
                description: GroupParameters are the configurable fields of a Group.
                properties:
                  description:
                    description: Description of the Group. This is an optional field
                      that provides additional information about the Group.
                    type: string
                  name:
                    description: Name of the Group. This is a required field and must
                      be unique across all Groups in SonarQube.
                    minLength: 3
                    type: string
                  permissions:
                    description: |-
                      Permissions is a list of permissions to be assigned to the Group. This is an optional field that specifies the permissions the Group should have in SonarQube.
                      Allowed values are "admin", "gateadmin", "profileadmin", "provisioning", "scan", "applicationcreator", "portfoliocreator"
                    items:
                      enum:
                      - admin
                      - gateadmin
                      - profileadmin
                      - provisioning
                      - scan
                      - applicationcreator
                      - portfoliocreator
                      type: string
                    type: array
                required:
                - name
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A GroupStatus represents the observed state of a Group.
            properties:
              atProvider:
                description: GroupObservation are the observable fields of a Group.
                properties:
                  default:
                    description: Default indicates whether the Group is a default
                      group in SonarQube.
                    type: boolean
                  description:
                    description: Description is the description of the Group as observed
                      from SonarQube. This field the actual description of the Group
                      in SonarQube.
                    type: string
                  id:
                    description: ID is the unique identifier of the Group in SonarQube.
                      This field is set by the SonarQube API.
                    type: string
                  managed:
                    description: Managed indicates if the group is created and managed
                      by SonarQube itself. Managed groups cannot be modified or deleted
                      by users, and are typically used for internal purposes within
                      SonarQube.
                    type: boolean
                  name:
                    description: Name is the name of the Group as observed from SonarQube.
                      Reflects the actual name of the Group in SonarQube.
                    type: string
                  permissions:
                    description: Permissions is a list of permissions assigned to
                      the Group as observed from SonarQube. This field reflects the
                      actual permissions the Group has in SonarQube.
                    items:
                      type: string
                    type: array
                required:
                - default
                - description
                - id
                - managed
                - name
                - permissions
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
